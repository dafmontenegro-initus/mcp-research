MCP_URL=http://localhost:8080
MCP_TOKEN=traj_<get from: cd ../trajectory-mcp && python3 manage_users.py token daniel.montenegro@initus.io>
OLLAMA_URL=http://localhost:11434

# Single-evaluator model. qwen3.6:27b plays both roles: it generates the test
# cases and then evaluates each result (intent + contract in one pass).
MODEL=qwen3.6:27b
